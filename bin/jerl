#!/bin/sh
#
# Wrapper to 'erl' that puts all the jungerl programs in the code path.
#
# Note: This script must be run from jungerl/bin/ -- put that in your
# path to use it!

basedir=$(dirname $(dirname $0))

erl -pa ${basedir}/lib/*/ebin "$@"

