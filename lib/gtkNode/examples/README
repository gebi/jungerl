start an erlang node like this;

cd gtkNode/examples
erl <-pa [a-z]*> -pa ../ebin -sname foo

 examples;

simple - a simple top-application
         start with simple:start().

points - draws (really small) points on a drawingarea
         start points:start().
